# AquaWatch-MS - Template des variables d'environnement
# =====================================================
# INSTRUCTIONS: 
# 1. Copiez ce fichier en .env : copy .env.template .env
# 2. Modifiez les valeurs selon votre environnement local
# 3. NE JAMAIS commiter le fichier .env (il est dans .gitignore)

# Variables système
USER=aquawatch

# Base de données TimescaleDB/PostgreSQL
POSTGRES_USER=aquawatch_user
POSTGRES_PASSWORD=AquaWatch2024!
POSTGRES_DB=aquawatch_db
POSTGRES_HOST=db_timescale
POSTGRES_PORT=5432

# MinIO (Stockage fichiers)
MINIO_ROOT_USER=aquawatch_admin
MINIO_ROOT_PASSWORD=AquaWatch2024MinIO!
MINIO_HOST=minio_storage
MINIO_PORT=9000
MINIO_BUCKET_NAME=aquawatch-data

# Services Ports
SERVICE_CAPTEURS_PORT=8001
SERVICE_SATELLITE_PORT=8002
SERVICE_STMODEL_PORT=8003
SERVICE_ALERTES_PORT=8004
SERVICE_API_SIG_PORT=8005

# APIs externes (remplacez par vos vraies clés)
SENTINEL_HUB_CLIENT_ID=your_sentinel_client_id
SENTINEL_HUB_CLIENT_SECRET=your_sentinel_client_secret
COPERNICUS_API_KEY=your_copernicus_api_key

# Notifications (Service Alertes) - remplacez par vos vraies valeurs
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMS_PROVIDER_API_KEY=your_sms_api_key

# Seuils d'alerte (normes OMS)
PH_MIN=6.5
PH_MAX=8.5
TURBIDITY_MAX=4.0
TEMPERATURE_MAX=25.0
CONDUCTIVITY_MAX=1000.0

# Machine Learning
ML_MODEL_PATH=/app/models
PREDICTION_HORIZON_HOURS=72
RETRAIN_INTERVAL_DAYS=7

# Development/Production
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO